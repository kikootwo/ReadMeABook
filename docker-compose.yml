version: '3.9'

services:
  # PostgreSQL Database
  postgres:
    image: postgres:16-alpine
    container_name: readmeabook-postgres
    restart: unless-stopped
    volumes:
      - pgdata:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: readmeabook
      POSTGRES_USER: readmeabook
      POSTGRES_PASSWORD: readmeabook_password_change_me
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U readmeabook"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - readmeabook-network

  # Redis Cache & Queue
  redis:
    image: redis:7-alpine
    container_name: readmeabook-redis
    restart: unless-stopped
    command: redis-server --appendonly yes
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - readmeabook-network

  # ReadMeABook Application
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: readmeabook-app
    restart: unless-stopped
    ports:
      - "3030:3030"
    volumes:
      - ./config:/app/config
      - ./downloads:/downloads
      - ./media:/media
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      # Database Configuration
      DATABASE_URL: postgresql://readmeabook:readmeabook_password_change_me@postgres:5432/readmeabook

      # Redis Configuration
      REDIS_URL: redis://redis:6379

      # JWT Secrets
      JWT_SECRET: XdiVg4yJjlabEGsJFfmRQoTTLfIWetvOsx2ToqYiUSg=
      JWT_REFRESH_SECRET: pqj7mGktILiLYrMAJVNJ4NKz9gU2jX4oRcTlCgFyLrA=

      # Encryption Key (for storing sensitive config in database)
      CONFIG_ENCRYPTION_KEY: XKYTHVE1FST3LUycTmmrcnvFmJA+eFLzDNHiKb7Fqw0=

      # Plex Configuration
      PLEX_CLIENT_IDENTIFIER: readmeabook-unique-client-id
      PLEX_PRODUCT_NAME: ReadMeABook

      # Node Environment
      NODE_ENV: production

      # Logging
      LOG_LEVEL: info

      # Application Settings
      PORT: 3030
      HOSTNAME: 0.0.0.0
    networks:
      - readmeabook-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3030/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

networks:
  readmeabook-network:
    driver: bridge

volumes:
  pgdata:
    driver: local
  redisdata:
    driver: local
